
import React from "react";
import ReactDOM from "react-dom/client";
import LoginPage from "./src/bce/LoginPage";

const root = ReactDOM.createRoot(document.getElementById("root")!);
root.render(
  <React.StrictMode>
    <LoginPage />
  </React.StrictMode>
);
